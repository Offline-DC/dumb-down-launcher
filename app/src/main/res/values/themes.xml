<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DumbDownLauncher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Drawable instead of @color/black so the cold-start transition
             shows the duck centered on black (matches BootRegistrationScreen)
             rather than a several-hundred-ms all-black flash. -->
        <item name="android:windowBackground">@drawable/splash_window_background</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <style name="Theme.DumbDownLauncher" parent="Base.Theme.DumbDownLauncher" />
</resources>